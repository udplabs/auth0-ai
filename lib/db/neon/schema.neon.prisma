generator client {
  provider        = "prisma-client-js"
  output          = "../generated/neon"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider  = "postgresql"
  url       = env("NEON_URL")
  directUrl = env("NEON_URL_UNPOOLED")
}
