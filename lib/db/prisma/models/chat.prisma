model Chat {
  id        String    @id @default(ulid())
  userId    String?
  title     String?
  messages  Message[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  @@index([userId])
}
